# GAP-420: Update Template CI — owner choose A or B

Owner decision for Joshua. This note does not choose an option, does not create a token, and does not change any workflow file.

## Current file (fact only)

`.github/workflows/updates.yml` on `master` is a comment-only block dated 2026-08-15. It records that Upptime "Update Template" CI historically used the floating third-party ref `upptime/updates@master`, and that `.upptimerc.yml` already treats template auto-update as a manual event. This note does not treat that comment as the owner's decision and does not edit the file.

## Options

Pick one:

- **(A)** Joshua creates a fine-grained PAT scoped to RevealUIStudio/status only (and wires it per Upptime docs), OR
- **(B)** Retire the Update Template workflow permanently (confirm retired / remove schedule if still live — owner decides).

Bot must not create a PAT, must not pick A or B, and must not edit workflow YAML.
